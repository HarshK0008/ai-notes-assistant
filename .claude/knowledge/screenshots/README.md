# Screenshots

UI mockups or references fed to the AI as visual grounding for a feature.
These are not auto-loaded — reference a specific image from a feature spec's
`## References` section (or paste it directly into a conversation) when it's
relevant to the work at hand.

This folder starts empty — add images here as visual references are created.
