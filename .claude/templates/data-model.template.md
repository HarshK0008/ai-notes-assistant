# Data Model

## Overview

<!-- One or two sentences: what kind of data does this system store, and how do the main entities relate? -->

## Entities

<!-- One subsection per entity. Name, fields, relationships to other entities. -->

### <EntityName>

- **Fields**: <field: type, field: type, ...>
- **Relationships**: <e.g. belongs to X, has many Y>

## Schema

<!-- Optional: paste the actual schema (SQL DDL, Prisma schema, ORM models, etc.) once it exists. Keep this in sync with the real source of truth — don't let it drift. -->

```
<schema here>
```
