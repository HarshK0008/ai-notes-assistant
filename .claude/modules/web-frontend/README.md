# Module: web-frontend (placeholder)

**Status**: Not yet built. Planned content below.

## Planned

An `agents/ui-reviewer.md` subagent that uses a browser-automation MCP server
(e.g. Playwright) to review a running web UI for:

- Layout, spacing, and typography issues
- Responsiveness across mobile/tablet/desktop breakpoints
- Accessibility (alt text, focus states, contrast, tap target size)

## How to Enable (once built)

Copy `agents/ui-reviewer.md` into `.claude/agents/`. Requires a browser
automation MCP server to be configured for the project.
